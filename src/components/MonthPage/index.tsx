import React from 'react';

export interface MonthPageProps {
}

// eslint-disable-next-line no-empty-pattern
const MonthPage: React.FC<MonthPageProps> = ({
}: MonthPageProps): JSX.Element => (
  <div>
    Month
  </div>
);

export default MonthPage;
