import React from 'react';

export interface YearPageProps {
}

// eslint-disable-next-line no-empty-pattern
const YearPage: React.FC<YearPageProps> = ({
}: YearPageProps): JSX.Element => (
  <div>
    Year
  </div>
);

export default YearPage;
